<form method="post" action="{{ path('admin_pams_code_delete', {'id': pams_code.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pams_code.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
